import tkinter
from tkinter import * 
# Will import everything from the tkinter library

window = Tk() #Creates a window

window.mainloop() #Will put the window into a listening mode so that it waits for an event to happen. Without it will instantly close, this ensures the program remains open



